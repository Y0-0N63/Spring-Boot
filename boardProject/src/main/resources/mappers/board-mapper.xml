<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!-- namespace에 연결할 Mapper 인터페이스의 패키지명.인터페이스명 작성
	> Mapper 인터페이스에 있는 메서드 호출 시, 해당 xml 파일에 작성된 태그 중 메서드명과 동일한 id를 가진 sql 수행  -->
<mapper namespace="edu.kh.project.board.model.mapper.BoardMapper">

	<!-- 게시판 종류 조회 -->
	<select id="selectBoardTypeList">
		SELECT BOARD_CODE "boardCode", BOARD_NAME "boardName"
		FROM "BOARD_TYPE"
		ORDER BY BOARD_CODE
	</select>
	
	<!-- 게시글 수 조회 -->
	<select id = "getListCount">
		SELECT COUNT(*)
		FROM "BOARD"
		WHERE BOARD_DEL_FL = 'N'
		AND BOARD_CODE = #{boardCode}
	</select>
	
	<!-- 특정 게시판의 지정된 페이지 목록 조회 
		CASE-WHEN 구문에서 사용한 '<'가 오류를 일으킴 > tag로 인식 중이기 때문! > 문자열임을 알려줘야 함 (= 순수 문자 데이터임을 지정해줘야 함)
		<![CDATA["문자열"]]> : 해당 태그 내부에 작성된 문자열 : 태그와 같은 특수 기호로 해석 X, 문자 그대로 해석해야 함을 알려줌(명령) -->
	<select id="selectBoardList">
		SELECT BOARD_NO, BOARD_TITLE, MEMBER_NICKNAME, READ_COUNT,
		(SELECT COUNT(*) FROM "COMMENT" C
		WHERE C.BOARD_NO = B.BOARD_NO) COMMENT_COUNT,
		(SELECT COUNT(*) FROM "BOARD_LIKE" L
		WHERE L.BOARD_NO = B.BOARD_NO) LIKE_COUNT,
		<![CDATA[
		CASE
			WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 / 60
			THEN FLOOR((SYSDATE - BOARD_WRITE_DATE) * 24 * 60 * 60) || '초 전'
			WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24
			THEN FLOOR((SYSDATE - BOARD_WRITE_DATE) * 24 * 60) || '분 전'
			WHEN SYSDATE - BOARD_WRITE_DATE < 1
			THEN FLOOR((SYSDATE - BOARD_WRITE_DATE) * 24) || '시간 전'
			ELSE TO_CHAR(BOARD_WRITE_DATE, 'YYYY-MM-DD')
		END BOARD_WRITE_DATE
		]]>
		FROM "BOARD" B
		JOIN "MEMBER" USING(MEMBER_NO)
		WHERE BOARD_DEL_FL = 'N'
		AND BOARD_CODE = #{boardCode}
		ORDER BY BOARD_NO DESC
	</select>
	
	<!-- 동적 SQL 사용하기 : <choose>, <when>, <otherwise>
	- <choose> : 조건문을 작성할 영역 지정, 내부에 <when>, <otherwise> 작성 가능
	- <when> : if, else if 역할의 태그, test라는 속성에 조건식 작성   - <otherwise> : else 역할의 태그 -->
	
	<!-- 검색 조건이 맞는 게시글 수 조회 -->
	<select id = "getSearchCount">
		SELECT COUNT(*) FROM "BOARD"
		
		<!-- 작성자 검색인 경우 > 테이블 간 JOIN 필요 -->
		<!-- 내부 문자열을 "으로, 외부 조건식을 '로 바꿔주기 (XML 파서의 혼동을 막아주기 위해) -->
		<if test='key == "w"'>
			JOIN "MEMBER" USING(MEMBER_NO)
		</if>
		
		WHERE BOARD_DEL_FL = 'N'
		AND  BOARD_CODE = #{boardCode}
		
		<choose>
			<!-- 제목 검색인 경우 : key 값 "t"-->
			<!-- '%'||#{query}||'%' : 검색어 패턴 만들기 (query 변수 값을 받아준 후 > %와 문자열 이어쓰기) -->
			<when test='key == "t"'>
				AND BOARD_TITLE LIKE '%'||#{query}||'%'
			</when>
			
			<!-- 내용 검색인 경우 : key 값 "c" -->
			<when test='key == "c"'>
				AND BOARD_CONTENT LIKE '%'||#{query}||'%'
			</when>
			
			<!-- 제목+내용 검색 : key 값 "tc" -->
			<when test = 'key =="tc"'>
				AND (
					BOARD_CONTENT LIKE '%'||#{query}||'%'
					OR
					BOARD_TITLE LIKE '%'||#{query}||'%'
				)
			</when>
			
			<!-- 작성자 검색인 경우 : key 값 "w"-->
			<otherwise>
				AND MEMBER_NICKNAME LIKE '%'||#{query}||'%'			
			</otherwise>
		</choose>
	</select>
	
	<!-- 검색 결과 목록 조회 -->
	<select id="selectSearchList">
		SELECT BOARD_NO, BOARD_TITLE, MEMBER_NICKNAME, READ_COUNT,
		(SELECT COUNT(*) FROM "COMMENT" C
		WHERE C.BOARD_NO = B.BOARD_NO) COMMENT_COUNT,
		(SELECT COUNT(*) FROM "BOARD_LIKE" L
		WHERE L.BOARD_NO = B.BOARD_NO) LIKE_COUNT,
		<![CDATA[
		CASE
			WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 / 60
			THEN FLOOR((SYSDATE - BOARD_WRITE_DATE) * 24 * 60 * 60) || '초 전'
			WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24
			THEN FLOOR((SYSDATE - BOARD_WRITE_DATE) * 24 * 60) || '분 전'
			WHEN SYSDATE - BOARD_WRITE_DATE < 1
			THEN FLOOR((SYSDATE - BOARD_WRITE_DATE) * 24) || '시간 전'
			ELSE TO_CHAR(BOARD_WRITE_DATE, 'YYYY-MM-DD')
		END BOARD_WRITE_DATE
		]]>
		FROM "BOARD" B
		JOIN "MEMBER" USING(MEMBER_NO)
		WHERE BOARD_DEL_FL = 'N'
		AND BOARD_CODE = #{boardCode}
		<choose>
			<when test='key == "t"'>
				AND BOARD_TITLE LIKE '%'||#{query}||'%'
			</when>
			
			<when test='key == "c"'>
				AND BOARD_CONTENT LIKE '%'||#{query}||'%'
			</when>
			
			<when test = 'key =="tc"'>
				AND (
					BOARD_CONTENT LIKE '%'||#{query}||'%'
					OR
					BOARD_TITLE LIKE '%'||#{query}||'%'
				)
			</when>
			
			<otherwise>
				AND MEMBER_NICKNAME LIKE '%'||#{query}||'%'			
			</otherwise>
		</choose>
		ORDER BY BOARD_NO DESC
	</select>
	
	<!-- 게시글 상세 조회
		id가 selectOne인 SQL문 조회 > board_rm에 담아 > <resultMap>으로 > <resultMap> 안의 <collection>을 통해 나머지 SQL을 수행 -->
	<resultMap type="Board" id="board_rm">
		<!-- resultMap 태그의 역할
			1) 조회된 컬럼명(BOARD_NO) DTO의 필드명(boardNo)이 일치하지 않을 때 매핑시켜주는 역할
			(mybatis-config에 작성한 mapUnderscoreToCamelCase를 통해 컬럼명-필드명을 자동으로 매핑했던 것을 수동으로 연결하는 셈)
			id 태그 : PK를 연결, result 태그 : PK가 아닌 일반 컬럼을 연결 -->
		<id property="boardNo" column="BOARD_NO" />
		<!-- <result property="boardTitle" column="BOARD_TITLE"/> : 하지 않아도 무관 -->
		
		<!-- 2) <collection> 태그를 추가 작성하여 여러 행의 결과가 조회되는 다른 SELECT문을 수행한 후 > Collection에 담아
		 > 그 결과를 지정된 DTO의 필드(property에 작성된 필드명을 통해)에 대입(세팅)
		 	property : List를 담을 DTO의 필드명
			select : 실행할 select 의 id
			column : 조회 결과(부모쿼리 수행결과) 중 지정된 컬럼의 값을 파라미터로 전달
			javaType : List(컬렉션)의 타입을 지정
			ofType : List(컬렉션)의 제네릭(타입제한) 지정 -->
		<!-- imgList(BoardDTO의 private List<BoardImg> imageList) 조회 -->
		<collection
			property="imageList"
			column="BOARD_NO"
			select="selectImageList"
			javaType="java.util.ArrayList"
			ofType="BoardImg"
		 />
		
		<!-- commentList(private List<Comment> commentList) 조회 --> 
		<collection
			property="commentList"
			column="BOARD_NO"
			select="selectCommentList"
			javaType="java.util.ArrayList"
			ofType="Comment"
		/> 
	</resultMap>
	
	<!-- resultMap의 collection 태그를 이용한 SQL 호출 시
	 > 반환 타입은 반드시 resultType 속성을 이용해 명시해야 함 (추론 불가) -->
	<!-- 상세 조회한 게시글의 이미지 목록 조회 -->
	<select id="selectImageList" resultType="BoardImg">
		SELECT * FROM "BOARD_IMG"
		WHERE BOARD_NO = #{boardNo}
		ORDER BY IMG_ORDER
	</select>
	
	<!-- 상세 조회한 게시글의 댓글 목록 조회(계층형 쿼리) -->
	<select id="selectCommentList" resultType="Comment">
		SELECT LEVEL, C.* FROM
		(SELECT COMMENT_NO, COMMENT_CONTENT,
		TO_CHAR(COMMENT_WRITE_DATE, 'YYYY"년" MM"월" DD"일" HH24"시" MI"분" SS"초"') COMMENT_WRITE_DATE,
		BOARD_NO, MEMBER_NO, MEMBER_NICKNAME, PROFILE_IMG, PARENT_COMMENT_NO, COMMENT_DEL_FL
		FROM "COMMENT"
		JOIN MEMBER USING(MEMBER_NO)
		WHERE BOARD_NO = 2000) C
		WHERE COMMENT_DEL_FL = 'N'
		OR 0 != (SELECT COUNT(*) FROM "COMMENT" SUB
			WHERE SUB.PARENT_COMMENT_NO = C.COMMENT_NO
			AND COMMENT_DEL_FL = 'N')
			START WITH PARENT_COMMENT_NO IS NULL
		CONNECT BY PRIOR COMMENT_NO = PARENT_COMMENT_NO
		ORDER SIBLINGS BY COMMENT_NO
	</select>
	
	<!-- resultMap = "board_rm" : resutlMap 태그로 만든 id가 board_rm에 selectOne의 결과를 매핑해줌 -->
	<select id="selectOne" resultMap = "board_rm">
		SELECT BOARD_NO, BOARD_TITLE, BOARD_CONTENT, BOARD_CODE, READ_COUNT,
		MEMBER_NO, MEMBER_NICKNAME, PROFILE_IMG,
		TO_CHAR(BOARD_WRITE_DATE, 'YYYY"년" MM"월" DD"일" HH24:MI:SS') BOARD_WRITE_DATE,
		TO_CHAR(BOARD_UPDATE_DATE, 'YYYY"년" MM"월" DD"일" HH24:MI:SS') BOARD_UPDATE_DATE,
		
		(SELECT COUNT(*)
		FROM "BOARD_LIKE"
		WHERE BOARD_NO = #{boardNo}) LIKE_COUNT,
		
		(SELECT IMG_PATH || IMG_RENAME
		FROM "BOARD_IMG"
		WHERE BOARD_NO = #{boardNo}
		AND IMG_ORDER = 0 ) THUMBNAIL,
		
		(SELECT COUNT(*)
		FROM "BOARD_LIKE"
		WHERE BOARD_NO = #{boardNo}
		AND MEMBER_NO = #{memberNo}) LIKE_CHECK
		
		FROM "BOARD"
		JOIN "MEMBER" USING(MEMBER_NO)
		WHERE BOARD_DEL_FL = 'N'
		AND BOARD_CODE = #{boardCode}
		AND BOARD_NO = #{boardNo}
	</select>
</mapper>